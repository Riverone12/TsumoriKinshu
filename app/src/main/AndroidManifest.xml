<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="biz.riverone.tsumorikinshu">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".ApplicationControl"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.CalendarActivity"
            android:theme="@style/AnimatedTheme" />
        <activity android:name=".views.PreferenceActivity" />
        <activity
            android:name=".views.PreferenceSubActivity"
            android:theme="@style/TransparencyTheme" />
        <activity
            android:name=".views.WizardActivity"
            android:theme="@style/TransparencyAnimatedTheme" />
        <activity
            android:name=".views.MemoActivity"
            android:label="@string/title_activity_memo"
            android:theme="@style/AppTheme" />
    </application>

</manifest>